ssh_user: []                  # to generate ~/.ssh dir and ~/.ssh/config client based on the 'owner' key
#   - owner: luke             # required
#     group: wheel            # optional
#     path:  custom/ssh       # optional, relative path of client ssh (ex: /home/luke/custom/ssh)
